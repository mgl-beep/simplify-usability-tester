
  # SIMPLIFY LTI (Cavas Login)

  This is a code bundle for SIMPLIFY LTI (Cavas Login). The original project is available at https://www.figma.com/design/QWoL07cvCApVzWaigMbMja/SIMPLIFY-LTI--Cavas-Login-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  